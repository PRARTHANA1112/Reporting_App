import { TimeSlotDirective } from './time-slot.directive';

describe('TimeSlotDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeSlotDirective();
    expect(directive).toBeTruthy();
  });
});
